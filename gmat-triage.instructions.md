---
description: "GMAT Agent 分流决策树 — 根据用户自然语言描述确定执行路径。当用户提出航天任务仿真需求时，按此决策树逐层分析：GUI需求、空间范围、任务类型、输出要求，未明确部分提供选项给用户。"
---

# GMAT Agent 分流决策树

当用户以自然语言描述航天任务仿真需求时，严格按照以下 5 层决策树分析需求。**每一层若用户未明确，必须列出选项供用户选择，不得猜测默认值。**

---

## 第 1 层：GUI 需求

**判断**: 用户是否需要 3D 可视化或 GMAT GUI 交互？

| 用户意图 | 关键词 | 处理方式 |
|----------|--------|----------|
| 需要 3D 可视化 | "3D"、"可视化"、"看轨道"、"动画"、"OpenFrames" | 生成脚本添加 `OpenFramesInterface` 块；告知用户需在 GMAT GUI 中打开脚本查看 |
| 不需要 GUI | 默认（大多数纯计算场景） | 仅使用 Python API 执行，返回结构化数据 |

**歧义处理**: 若用户未提及可视化，默认走非 GUI 路径。但告知用户："生成的 `.script` 文件可在 GMAT GUI 中打开查看 3D 轨道。"

---

## 第 2 层：空间范围

**判断**: 任务涉及的天体范围决定了使用的坐标系和引力模型。

| 范围 | 涉及天体 | 状态 | 支持情况 |
|------|----------|------|----------|
| **地球** (Earth) | 仅地球 | ✅ 当前支持 | 完整：LEO/GEO/HEO、大气阻力、J2 摄动、地面站 |
| **地月系** (Cislunar) | 地球 + 月球 (Luna) | 🔮 预留 | 架构就位，脚本模板待验证：月球转移、 swingby、拉格朗日点 |
| **行星际** (Interplanetary) | 地球 + 其他行星 | 🔮 预留 | 架构就位，脚本模板待验证：火星 B-plane、行星际转移、发射窗口 |

**坐标系选择**:
- 地球: `EarthMJ2000Eq` (默认), `EarthFixed`, `EarthICRF`
- 地月系: `EarthMJ2000Eq` + `Luna` 为 PointMass，或使用 `MoonMJ2000Eq`
- 行星际: `EarthMJ2000Eq` + 目标天体坐标系，使用 `SolarSystemBarycenter`

**歧义处理**: 若用户只说"轨道"而未指定天体，列出当前支持的选项（地球），并提示地月系和行星际为预留功能。

---

## 第 3 层：任务类型

**判断**: 用户要做什么？

| 类型 | 子类型 | 关键词 | 路由到 |
|------|--------|--------|--------|
| **简单传播** | 纯轨道预测 | "仿真"、"传播"、"轨道预报"、"运行N天" | 生成基础 `.script` → `scripts/runner/python_runner.py` |
| **变轨/转移** | 脉冲/有限推力 | "变轨"、"转移"、"Hohmann"、"delta-V"、"ΔV" | 生成含 `Target`/`Achieve`/`Vary` 的脚本 → runner |
| **参数扫描** | 多参数变化 | "扫描"、"sweep"、"不同倾角"、"多种初始状态" | 使用 `parameterized_propagation.script` 模板 + `--var` |
| **OEM 分析** | 已有轨道数据 | "OEM 文件"、"轨道数据"、"星历"、"分析趋势" | `scripts/analysis/oem_reader.py` → `scripts/analysis/plot_altitude.py` |
| **变轨检测** | 从 OEM 找机动 | "检测变轨"、"机动检测"、"轨道变化" | `scripts/analysis/maneuver_detector.py` |
| **发射窗口** | 交会窗口预测 | "发射窗口"、"神舟"、"天舟"、"过顶"、"交会" | `scripts/prediction/launch_window.py` |

### 参考脚本速查

| 场景 | 推荐参考脚本 |
|------|-------------|
| 基础传播入门 | `references/templates/simple_propagation.script` |
| 参数化传播 | `references/templates/parameterized_propagation.script` |
| Hohmann 转移 | `references/templates/impulsive_targeting.script` |
| 有限推力 | `references/templates/finite_burn.script` |
| 更多官方示例 | `references/samples/INDEX.md` |

**歧义处理**: 若用户需求模糊（如"分析一下这个轨道"），追问：要传播预测？还是要从已有数据中提取信息？

---

## 第 4 层：输出要求

**判断**: 用户需要什么形式的输出？

| 输出类型 | 用户可能说 | 处理方式 |
|----------|-----------|----------|
| **轨道根数** | "轨道参数"、"轨道六根数"、"SMA"、"偏心率" | `python_runner.py --objects Sat` → 读取 Keplerian 状态 |
| **位置速度时间序列** | "完整轨迹"、"每个时间步"、"星历表" | 脚本添加 `ReportFile` → 解析输出 |
| **ΔV 预算** | "需要多少 ΔV"、"燃料预算"、"速度增量" | 从 Target 求解器结果中提取 |
| **高度图** | "画轨道高度"、"近地点远地点" | `scripts/analysis/plot_altitude.py` |
| **过顶时刻表** | "什么时候过顶"、"AOS/LOS"、"可见弧段" | `scripts/prediction/launch_window.py --json` |
| **变轨报告** | "哪几次变轨"、"机动时刻和大小" | `scripts/analysis/maneuver_detector.py` |

**组合输出**: 用户可以同时要求多种输出。每种输出独立处理，并行返回结果。

**歧义处理**: 若用户只说"给我结果"，默认返回轨道根数摘要。同时询问是否需要图形、时刻表等。

---

## 第 5 层：参数歧义检测

**检查以下参数是否已明确**。未明确的参数，列出选项给用户：

| 参数 | 检查项 | 常见歧义 | 默认值 (仅地球轨道) |
|------|--------|----------|---------------------|
| **轨道** | 高度/半长轴？倾角？偏心率？ | "低轨道"没有具体数值 | 不设默认，必须明确 |
| **时间** | 起始历元？传播时长？ | "几天"没有具体数值 | 当前日期 + 3 天 |
| **航天器** | 质量？面积？阻力系数？ | 未提则用默认 | 850 kg, Cd=2.2, Cr=1.8 |
| **输出格式** | JSON？文本？图形？ | 未提 | JSON + 自然语言摘要 |
| **坐标系** | MJ2000Eq？Fixed？ | 未提则按空间范围默认 | EarthMJ2000Eq |

### 歧义处理原则

1. **关键物理参数缺失**（轨道高度、目标轨道等）→ **必须追问**，不生成脚本
2. **次要参数缺失**（航天器质量、面积等）→ 使用默认值，告知用户
3. **输出形式不明** → 默认返回 JSON + 自然语言，提示可选的图形/表格
4. **用户提供 OEM 文件** → 先解析文件内容，向用户确认数据概要后再决定后续操作

---

## 完整决策流程示意

```
用户输入
    │
    ▼
[1] GUI 需求？ ──Yes──→ 添加 OpenFramesInterface 块
    │ No
    ▼
[2] 空间范围？ ──地月系/行星际──→ 提示：预留功能，是否继续尝试？
    │ 地球
    ▼
[3] 任务类型？
    ├── 简单传播 ──→ 生成脚本 → runner
    ├── 变轨/转移 ──→ 生成 Target 脚本 → runner
    ├── 参数扫描 ──→ 模板 + --var → runner
    ├── OEM 分析 ──→ oem_reader → plot_altitude
    ├── 变轨检测 ──→ maneuver_detector
    └── 发射窗口 ──→ launch_window
    │
    ▼
[4] 输出要求？──→ 轨道根数 / 完整轨迹 / 图形 / 时刻表
    │
    ▼
[5] 参数完整？ ──No──→ 列出缺失参数选项，等待用户确认
    │ Yes
    ▼
执行 → 返回结果
```
